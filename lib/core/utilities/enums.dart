enum PieceColor { white, black }

enum StraightDirection {
  horizontalRight,
  horizontalLeft,
  verticalTop,
  verticalBottom,
}

enum DiagonalDirection {
  topLeft,
  topRight,
  bottomLeft,
  bottomRight,
}

enum KnightDirection {
  topRight,
  topLeft,
  bottomRight,
  bottomLeft,
  rightTop,
  rightBottom,
  leftTop,
  leftBottom,
}
